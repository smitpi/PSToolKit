name: Run PSGallery Reports

on:
  workflow_dispatch:
  schedule:
    - cron: "50 9,17,1 * * *"
jobs:
  RunPWSH:
    runs-on: ubuntu-latest
    steps:
      - name: Run Gather stats script
        shell: pwsh
        run: |
          import-module ./PSToolKit/Public/Get-MyPSGalleryStat.ps1
          import-module ./PSToolKit/Public/Write-Message.ps1 -Force
          Get-MyPSGalleryStat -GitHubUserID ${{secrets.GH_USER}} -GitHubToken ${{secrets.GH_TOKEN}}